server:
  port: ${PORT:8080}


#spring:
# datasource:
 #   url: jdbc:mysql://localhost:3306/secure_notes_db?useSSL=false&serverTimezone=UTC
  #  username: root
   # password: anuragjadoun024@gmail
    #driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - email
              - profile
